# marty::format::LocaleInfoType перечисление

Базовый тип: `std::uint32_t`.

<val-list title="Имя|Значение|Описание">

- invalid,unknown
(std::uint32_t)(-1)
Недопустимое/неизвестное значение.

- invariant,c
0x00
Инвариантная (неизменная) локаль ("C"-локаль).

- user
0x01
Пользовательская локаль в операционной системе.

- system
0x02
Системная локаль (локаль операционной системы).

</val-list>
