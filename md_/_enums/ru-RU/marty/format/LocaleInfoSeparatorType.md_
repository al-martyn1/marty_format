# marty::format::LocaleInfoSeparatorType перечисление

Базовый тип: `std::uint32_t`.

<val-list title="Имя|Значение|Описание">

- invalid,unknown
(std::uint32_t)(-1)
Недопустимое/неизвестное значение.

- thousands
0x01
Separately for each numeral system.

- fractional
0x02
same as decimal, but for fractional part of floating point numbers.

</val-list>
